// This is the entry file of the scripting pack.

// Import (contents of) other script file by using import syntax:
// https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/import

// Native Script API modules can be imported,
// e.g. import { world } from '@minecraft/server'
// Note that node's native modules (such as crypto, fs) is unavailable at this environment

// To compile TypeScript files, use `tsc` command (or `npx tsc` if it's installed locally),
// or `tsc -w` to compile after file changes

console.log('Hello World!')
